# Amazon Wishlist Sale Picker — 開発ロードマップ

> 最終更新: 2026-04-27 / 現在バージョン: v0.2.0

---

## 現状 (v0.2.0)

| 機能 | 状態 |
|------|------|
| セール判定（バッジ・打ち消し価格・価格下落・キーワード） | ✅ 実装済み |
| 全件自動スクロール（MutationObserver 駆動） | ✅ 実装済み |
| 読み込み中オーバーレイ（進捗表示） | ✅ 実装済み |
| 割引率フィルター（popup スライダー） | ✅ 実装済み |
| jsdom 回帰テスト（fixtures 3 件） | ✅ 実装済み |
| アイコン（サークルデザイン） | ✅ v0.2 で更新 |
| Chrome Web Store 公開 | ❌ 未対応 |

---

## v0.2 — 品質・安定性 ✅ 完了

**目標**: Amazon の DOM 変更に強くする。日常使いできる完成度にする。

### DOM 耐性の強化

Amazon は DOM 構造を変更することがある。新パターンを発見したら即フィクスチャ化して回帰検証に追加する運用を継続する。

- [ ] fixtures を定期的に最新 DOM で更新する（目安: 月 1 回）
- [ ] detectSale が誤検知・見逃しを起こしたら `shared/detect.js` を修正 → `node verify-detect.mjs` で全件 PASS を確認
- [ ] 追加すべきパターン候補（DOM サンプルが必要）:
  - `a-badge` 系の新バッジ（現在未対応）
  - 「クーポン」ラベルのある商品
  - プライム会員限定価格の表示形式が変わった場合

参照: [fixtures/HOW_TO_CAPTURE.md](fixtures/HOW_TO_CAPTURE.md)

### 既知の制約の解消

- [x] **アイコン差し替え** — サークル型デザインに更新（v0.2.0）
- [x] **エラーハンドリング強化** — `updateUI()` の null deref を optional chaining で保護（v0.2.0）
- [x] **detectSale 例外時のデータセット一貫性** — 例外時も `wspSale='0'` をセットするよう修正（v0.2.0）
- [x] **`getTotalItemCount()` フォールバック** — テキスト解析が外れた場合に li 件数を返すよう追加（v0.2.0）

---

## v0.3 — 機能追加

**目標**: フィルター機能を拡充してより実用的にする。

- [ ] **価格帯フィルター** — 「○円以下の商品のみ」など価格帯で絞り込む
- [ ] **セール件数バッジ** — 拡張アイコンに検出件数のバッジを表示する（`chrome.action.setBadgeText`）
- [ ] **結果のソート** — 割引率の高い順にウィッシュリスト内で並び替える

---

## Chrome Web Store 公開に向けた準備

公開するには以下が必要。

| 作業 | メモ |
|------|------|
| アイコン正式化 | 16 / 48 / 128px、ストアの画像ガイドラインに準拠 |
| プロモーション画像 | 1280×800px のスクリーンショット（最低 1 枚） |
| プライバシーポリシー | 「収集するデータなし」でシンプルに書く。公開 URL が必要 |
| `host_permissions` 見直し | 現状は content script の `matches` のみ。fetch が必要になったら追加 |
| ストア説明文 | 日本語・英語の両方があると望ましい |

> 参考: [developer.chrome.com/docs/webstore/publish](https://developer.chrome.com/docs/webstore/publish/)

---

## 技術的な負債・保留事項

| 項目 | 詳細 | 優先度 | 状態 |
|------|------|--------|------|
| `shared/detect.js` のテストカバレッジ | 現在 3 ケースのみ。新パターン発見のたびに追加 | 継続対応 | 運用中 |
| `getTotalItemCount()` の精度 | li フォールバックを追加済み。テキスト正規表現は Amazon リニューアル時に要確認 | 低 | ✅ v0.2 で改善 |
| content script のエラー境界 | detectSale 例外時に dataset を '0' でデフォルト設定するよう修正済み | 低 | ✅ v0.2 で解消 |

---

## バージョン履歴

| バージョン | 日付 | 内容 |
|-----------|------|------|
| v0.2.0 | 2026-04-27 | 品質・安定性改善。アイコン更新、null deref 修正、エラー境界強化、getTotalItemCount フォールバック追加 |
| v0.1.0 | 2026-04-27 | 初回リリース。セール判定・自動スクロール・割引率フィルター・jsdom 回帰テスト |
